# Production Environment Variables
# Copy this file to .env.production and customize for your environment

# Database Configuration
POSTGRES_DB=urban_golf
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# These are used in docker-compose.yml for production deployment
# No need for DATABASE_URL in production - it's built from the above values

# CORS - comma-separated allowed origins
ALLOWED_ORIGINS=https://sc.urban-golf.ch

# Email notification (Brevo SMTP - optional)
# BREVO_SMTP_USER=
# BREVO_SMTP_PASS=

# Admin notification email (optional)
# ADMIN_EMAIL=
